{
  "private": true,
  "name": "mail-parser",
  "version": "0.9.1",
  "description": "TS bindings and wasm support for Stalwart Lab's mail-parser",
  "workspaces": [
    "mail-parser-ts",
    "mail-parser-wasm"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/asimpletune/mail-parser.git"
  },
  "keywords": [
    "TS",
    "email",
    "bindings",
    "wasm"
  ],
  "author": "Spencer Scorcelletti",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/asimpletune/mail-parser/issues"
  },
  "homepage": "https://github.com/asimpletune/mail-parser#readme",
  "scripts": {
    "build:ts": "npm run --workspace mail-parser-ts build",
    "build:wasm": "npm run --workspace mail-parser-wasm build",
    "build": "npm run build:ts && npm run build:wasm",
    "pack:ts": "npm pack --workspace mail-parser-ts --pack-destination mail-parser-ts",
    "pack:wasm": "npm pack --workspace mail-parser-wasm --pack-destination mail-parser-wasm",
    "pack": "npm run pack:ts && npm run pack:wasm"
  }
}
